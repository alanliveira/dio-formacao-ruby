# Descrição

Criar um programa na linguagem Ruby que utiliza a função matemática potência

# Desafio

Criar um array vazio, para que o usuário insira 3 números e no final apareça o resultado desses 3 números elevados a 3ª potência.

# Como Usar

## Pré-requisitos

- Ruby 3.4.7 ou superior instalado
- Bundler instalado (`gem install bundle:2.6.9`)

## Instalação

1. Clone ou acesse o diretório do projeto:
```bash
cd dio-formacao-ruby/desafio-03
```

2. Instale as dependências do projeto:
```bash
bundle install
```

## Executando a Calculadora

Para executar o programa da calculadora em modo interativo:

```bash
ruby main.rb
```

## Executando os Testes

O projeto utiliza **RSpec** para testes automatizados. Para executar todos os testes:

```bash
bundle exec rspec
```

Para visualizar o resultado dos testes:

```bash
bundle exec rspec --format documentation
```

