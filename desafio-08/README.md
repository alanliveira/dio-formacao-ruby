# Descrição

Criar um Código utilizando os conhecimentos que aprendemos em aula.

# Desafio

Utilizando a Gem NOKOGIRI você deverá ler aquele mesmo arquivo html do site example.com, mas ao inves de utilizar de "ler" 'h1' você deverá retornar na tela o que se encontra em ʻpʼ.

# Como Usar

## Pré-requisitos

- Ruby 3.4.7 ou superior instalado
- Bundler instalado (`gem install bundle:2.6.9`)

## Instalação

1. Clone ou acesse o diretório do projeto:
```bash
cd dio-formacao-ruby/desafio-08
```

2. Instale as dependências do projeto:
```bash
bundle install
```

## Executando o projeto

Para executar o projeto em modo interativo:

```bash
ruby main.rb
```

## Executando os Testes

O projeto utiliza **RSpec** para testes automatizados. Para executar todos os testes:

```bash
bundle exec rspec
```

Para visualizar o resultado dos testes:

```bash
bundle exec rspec --format documentation
```

